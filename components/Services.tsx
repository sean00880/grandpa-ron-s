'use client';

// Re-export the new SOTA ServiceCards component as Services
// This maintains backwards compatibility while using the new design
export { ServiceCards as Services } from './ServiceCards';
export { default } from './ServiceCards';